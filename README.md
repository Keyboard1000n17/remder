# reMDer

There's many terminal markdown readers, but they all have one problem: it
doesn't look like how you would write it. For example, headings. They don't
render it bigger. Headings should be bigger. This is all reMDer wants to do. All
it wants to do is render headings bigger and support all kinds of markdown.
